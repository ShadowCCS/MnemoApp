namespace Mnemo.Core.Models;

/// <summary>
/// Coarse capability class for mapping local models (onboarding, routing). Not a benchmark score.
/// </summary>
public enum HardwarePerformanceTier
{
    Low = 0,
    Mid = 1,
    High = 2
}
